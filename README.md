# SQL Practice — 113 queries on real data (SQLite)

Interview-prep environment: five real Kaggle datasets loaded into five SQLite
databases, with 113 progressively harder queries that run and return real results.

**Verified: 113/113 execute, 0 errors, 0 empty results.** Every row and column of
all 11 source CSVs is loaded — nothing sampled or dropped.

## Documentation

| | |
|---|---|
| **[docs/QUESTIONS.html](docs/QUESTIONS.html)** | **Start here.** All 113 queries as business questions, tabbed by project, with per-level skills, the full schema map, result previews, and a link from each card into the SQL. |
| [docs/DATA_NOTES.md](docs/DATA_NOTES.md) | Every measured caveat — which dataset has no signal, which columns don't exist, which finding is an artifact. Read before quoting any result. |
| [docs/DATA_PROFILE.md](docs/DATA_PROFILE.md) | What each source CSV really contains: columns, types, null rates, cardinality. |
| [docs/RESULTS.md](docs/RESULTS.md) | Per-query pass/fail, row counts and timings (generated). |

**Live:** <https://prateeek73.github.io/SQL-Cheat-Sheet/docs/QUESTIONS.html>

> The page loads its content from `docs/data/*.json` at runtime, so browsers block
> it over `file://`. Either use the link above, or run `python -m http.server` from
> the repo root and open `/docs/QUESTIONS.html`. GitHub's plain file view shows
> HTML as source, so it won't render there either.

## Quick start

Datasets and databases aren't in the repo (~1.1 GB, regenerable). Build once:

```bash
conda activate sqlPract
pip install -r requirements.txt
kaggle auth login                  # browser OAuth, one time

python scripts/fetch.py            # download 5 Kaggle datasets  -> data/raw/
python scripts/build.py            # CSVs -> 5 SQLite databases  -> db/
python scripts/run.py              # run all 113 queries         -> docs/RESULTS.md
```

Already built? `python scripts/run.py`. Then open any `db/*.db` in DBeaver or
VSCode SQLTools and work through `queries/`.

No Kaggle account? Download the five ZIPs by hand into `data/_incoming/` —
`fetch.py` matches them by filename. (The Kaggle client is 2.x and no longer
reads `kaggle.json`; use `kaggle auth login` or `~/.kaggle/access_token`.)

## The five projects

| # | Database | Rows | Qs | Focus |
|---|---|---|---|---|
| 1 | `project_1_ecommerce.db` | 128,975 sale lines | 26 | Revenue, market basket, RFM, pricing, margin |
| 2 | `project_2_churn.db` | 7,043 customers | 20 | Churn, retention, risk scoring, ROI |
| 3 | `project_3_hr.db` | 1,000 employees | 21 | Pay equity, ranking, outliers, attrition |
| 4 | `project_4_food.db` | 20,000 orders | 21 | Ops analytics + **null-result detection** |
| 5 | `project_5_fraud.db` | 284,807 transactions | 25 | Precision/recall, thresholds, feature selection |

Beginner (1–6) → Intermediate (7–14) → Advanced (15–20) → Extended (21+).

## Layout

```
queries/        01_ecommerce  02_churn  03_hr  04_food_delivery  05_fraud  (.sql)
scripts/        fetch → build → run      (+ profile_data, derive, sqlutil)
docs/
  QUESTIONS.html    shell only, ~2 KB
  assets/           style.css · app.js   — renders the page at runtime
  data/             meta.json · schema.json · results.json
  DATA_NOTES.md · DATA_PROFILE.md · RESULTS.md
data/ db/       gitignored — regenerated by fetch.py and build.py
```

The questions page is split into a shell, assets and data. Nothing is baked into
the HTML, so regenerating `docs/data/*.json` updates the site without touching
any markup.

A query file maps to its database by its leading number
(`01_ecommerce.sql` → `db/project_1_ecommerce.db`); `run.py` finds them by glob.
Each database keeps the untouched source as `raw_*` tables beside the derived
ones, so any derivation can be checked against its source.

## Two things that will bite you

**Math functions.** Some queries use `sqrt()`, `power()` and `log()`. SQLite only
ships these when compiled with `SQLITE_ENABLE_MATH_FUNCTIONS`. Test with
`SELECT sqrt(4);` — the `sqlPract` env has them, Anaconda's bundled `sqlite3.exe`
does not. Details in [DATA_NOTES.md](docs/DATA_NOTES.md).

**The data is not what tutorials claim.** The fraud set has no merchants or
customers (only PCA components), the churn set has no `churn_date`, and the food
delivery set is essentially randomly generated. The queries were written against
what's actually there — [DATA_NOTES.md](docs/DATA_NOTES.md) has the measurements.

<sub>Built from a supplied package that could not run: its orchestrator aborted on
a wrong Kaggle API method, its query splitter matched nothing, it skipped two of
five projects, and its SQL was not SQLite. All rewritten here.</sub>
